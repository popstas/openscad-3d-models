{
  "name": "openscad-3d-models",
  "version": "1.0.0",
  "description": "A collection of small, self‑contained OpenSCAD models organized by date and slug. Each folder contains the source .scad and, optionally, exported .stl files.",
  "main": "index.js",
  "scripts": {
    "watch": "node --watch --env-file=.env --import tsx src/watch.ts",
    "render": "node --env-file=.env --import tsx src/render-once.ts",
    "render:all": "node --env-file=.env --import tsx src/render-once.ts --all",
    "build": "tsc -p .",
    "test": "node --import tsx --test src/core.test.ts",
    "create-project": "node --import tsx src/create-project.ts"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "bluebird": "^3.7.2",
    "jimp": "^1.6.0",
    "pngjs": "^7.0.0"
  },
  "devDependencies": {
    "@types/bluebird": "^3.5.42",
    "@types/node": "^24.3.0",
    "@types/pngjs": "^6.0.5",
    "tsx": "^4.20.5",
    "typescript": "^5.9.2"
  }
}
